arr = [1, 2, 3, 4, 5]

def showFirstElement(arr):
	print(arr[0]) # O(1) constant time -> time of executing does not change as the input size increases.
	print(arr[1]) # O(1)

showFirstElement(arr) # O(1)